apiVersion: v1
kind: Pod
metadata:
  name: final-bookstore
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: data
      mountPath: /data
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: final-pvc
